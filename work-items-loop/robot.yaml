tasks:
  Create outputs with files:
    robotTaskName: Get inputs and create outputs using file paths from payload
  Create outputs with files using helper:
    robotTaskName: Get inputs and create outputs using file paths from payload with helper
  Create outputs with payload using helper:
    robotTaskName: Read work item with attached file and add content as payload
  Get payload given e-mail process triggering:
    robotTaskName: Get payload given e-mail process triggering
  Break Work Items Loop:
    robotTaskName: Break Work Items Loop

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore

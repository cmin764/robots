tasks:
  venv:
    shell: setupvenv.sh && which python
  which:
    shell: which python

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - bin
PYTHONPATH:
  - .
  - /Users/cmin/Repos/rpaframework/packages/main/src
ignoreFiles:
  - .gitignore

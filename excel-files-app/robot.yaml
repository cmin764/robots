tasks:
  Run all tasks:
    shell: python -m robot --report NONE --outputdir output --logtitle "Task log" tasks.robot
  Transplant Column:
    robotTaskName: Transplant Column
  Remove rows with empty cells:
    robotTaskName: Remove rows with empty cells
  Test single row sheet:
    robotTaskName: Test single row sheet
  Export CSV Table To Excel:
    robotTaskName: Export CSV Table To Excel
  Test Trailing Spaces:
    robotTaskName: Test Trailing Spaces

condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
ignoreFiles:
  - .gitignore
